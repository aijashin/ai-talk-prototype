# AI谈心谈话助手原型部署包

这是一个静态网页原型，包含：

- `index.html`
- `styles.css`
- `script.js`

## 推荐部署方式

### Netlify

打开 Netlify，新建站点，直接上传本文件夹 `deploy-static` 即可。

### Vercel

新建项目时选择静态项目，将本文件夹作为项目根目录部署即可。

### GitHub Pages

把这三个文件提交到仓库根目录或 `docs` 目录，并开启 GitHub Pages。

部署完成后，会得到一个公网访问地址，同事不需要在同一局域网也能访问。
